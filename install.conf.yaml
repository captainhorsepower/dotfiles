- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # ~/.gitconfig:
    ~/.zshenv: 
    ~/.zshrc: 

- create:
    - ~/Projects

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
