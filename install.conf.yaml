- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc: zshrc

- create:
    - ~/Projects

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
