- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # ~/.gitconfig:
    ~/.zshenv: 
    ~/.zshrc: 

- create:
    - ~/Projects

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
